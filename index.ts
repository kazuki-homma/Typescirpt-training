const person: {
    name: string;
    age: number;
} = {
    name: 'taketo',
    age: 13
}

const fruits: string[] = ['りんご', 'バナナ', 'ぶどう'];


console.log(`${person.name}は${person.age}才です。彼は${fruits[0]}が大好物です`);